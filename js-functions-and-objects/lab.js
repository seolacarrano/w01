// PREQUISITES:
// - if/else and switch statements
// - for and while loops
// - 'string'[0] or 'string'.charAt(0)
// - functions

/** 
 * 1. Write a function EXPRESSION called "favoriteAnimal" 
 * that accepts an animal as an argument and 
 * returns  "I like" + animal
 * 
 * It must use the function keyword
 * 
 * Ex: favoriteAnimal('cats') should return "I like cats!"
*/

// Create the function  below
// Call the function

/**
 * 2. Write a function EXPRESSION called "double", that takes 
 * in a number as an argument, doubles that number 
 * and returns the new number.
 * 
 * It must use the function keyword
 * 
 * Ex: double(3) should return 6
*/

// Create the function below
// Call the function

/**
 * 3. Write a function DECLARATION called "sum" that takes in two 
 * numbers as arguments and returns their sum
 * 
 * It must use the function keyword
 * 
 * Ex: sum(4, 5) should return  9 
 */

// Create the function below
// Call the function

/**
 * 4. Write a function DECLARATION called "coolEach" that takes in  
 * an array of numbers as an argument does the following:
 * - loops over the array
 * - multiplies each number by 2
 * - adds that number to counter
 * - returns the counter
 * 
 * The solution should:
 *  - use the Array.forEach() method
 * Ex: map([1,2,3]) should return 12
 */

// Create the function below
// Call the function

/**
 * 5. Write a function DECLARATION called "mapLength" that takes in  
 * an array of student name as an argument does the following:
 * - loops over the array
 * - returns the new array of the number of characters in each name
 * 
 * The solution should:
 *  - use the Array.map() method
 * Ex: map(['Suresh','Andrew','Casey']) should return [6,6,5]
 */

// Create the function below
// Call the function


/**
 * 6. Write a function DECLARATION called "filterNums" that takes in  
 * an array of numbers and a target number arguments and returns a new array only those numbers that are greater 
 * than the target number
 * 
 * The solution should:
 *  - use the Array.filter() method
 * 
 * Ex: filterNums([2,3,4,5], 3) should return [4,5]
 */

// Create the function below
// Call the function


/**
 * 7. Write a function called "isPalindrome" that takes in  
 * a string of text and console logs true if it's a 
 * palindrome and false if it's not.
 * 
 * The solution should incorporate an if/else statement 
 * and for loop
 * 
 * Ex: isPalindrome('racecar') should console log true
 */

// Create the function  below
// Call the function


/**
 * 8. Write a function called "calculate" that takes in
 * 2 numbers, a mathematical operation (+,-,*,/) and returns 
 * the value of that operation performed on the 2 numbers. 
 * 
 * The solution should incorporate a switch statement
 * 
 * Ex: calculate(1,2,add) should console log 3
 * Ex: calculate(1,2,substract) should console log 1
 */

// Create the function  below
// Call the function
